# Copy to .env (local dev) or .env.local (preview), or set in /var/www/ledger/.env on the VPS.
# Production default points at api.kynguyen.cc, so an empty .env still works against prod data.

# Dev pointing at locally-running x106-api:
# NUXT_PUBLIC_API_BASE=http://localhost:4000

NUXT_PUBLIC_API_BASE=https://api.kynguyen.cc
